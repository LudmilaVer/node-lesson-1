const logMessage = require('./logger'); // Импортируем функцию logMessage из файла logger.js

// Записываем несколько сообщений в лог
logMessage('This is the first log message');
logMessage('Another log message');
logMessage('Yet another log entry');
